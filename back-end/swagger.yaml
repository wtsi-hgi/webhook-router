swagger: "2.0"
info:
  version: 1.0.0
  title: Webhook router
  license:
    name: MIT
host: 127.0.0.1
paths:
  /route/{token}:
    parameters:
      - name: token
        in: path
        required: true
        description: The token of the route
        type: string
    post:
      summary: Location to send webhooks to, in order for them to be routed
      operationId: server.route_webhook
      responses:
        '200':
          description: Webhook routed correctly
        default:
          description: unexpected error
          schema:
            $ref: "#/definitions/Error"
  /view-routes:
    get:
      summary: Gets all routes that are configured for given user
      operationId: server.get_all_routes
      responses:
        '200':
          description: Returns the given routes
          schema:
            $ref: "#/definitions/Routes"
        default:
          description: unexpected error
          schema:
            $ref: "#/definitions/Error"
  /add-route:
    post:
      summary: Gets all routes that are configured
      operationId: server.add_route
      parameters:
        - name: new_route
          in: body
          required: true
          schema:
            $ref: "#/definitions/NewRoute"
      responses:
        '200':
          description: Expected response to a valid request
          schema:
            $ref: "#/definitions/Route"
        default:
          description: unexpected error
          schema:
            $ref: "#/definitions/Error"
definitions:
  NewRoute:
    required:
      - name
      - destination
      - write_users
    properties:
      name:
        type: string
      destination:
        type: string
      write_users:
        type: array
        items:
          type: string
  Route:
    allOf:
      - $ref: "#/definitions/NewRoute"
      - required:
          - token
        properties:
          token:
            type: string
  Routes:
    type: array
    items:
      $ref: "#/definitions/Route"
  Error:
    required:
      - code
      - message
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
