language: python
python: 3.6
install:
  - cd router
  - npm install
  - npm run-script build
  - cd ../config
  - pip install -r requirements.txt
  - pip install -r test_requirements.txt
script:
  - pytest test.py
  - npm test